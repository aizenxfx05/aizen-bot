# Fallback Railway Nixpacks build configuration when building from repo root
[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "cd bot && pip install -r requirements.txt"]

[start]
cmd = "cd bot && python CodeX.py"
